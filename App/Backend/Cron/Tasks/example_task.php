
<?php defined("ACCESS") or exit("Access Denied");

use Backend\Core\Cache;

// Example task for cron jobs
// Calculate PI and store it into cache

// $iterations = 10000;
// $value = 0.0;
// $sign = 1.0;
// $denominator = 1.0;

// for ($i = 0; $i < $iterations; $i++)
// {
//     $value += $sign * (1.0 / $denominator);
//     $denominator += 2.0;
//     $sign = -$sign;
// }

// $value = $value * 4.0;

// Cache::add("calculated_pi_value", $value, "1 day");
